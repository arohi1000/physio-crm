import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function WorkingHoursSettingsPage() {
  return (
    <PlaceholderPage
      title="Working hours"
      milestone="Milestone 2"
      summary="The working-hours template the slot engine computes availability from. doctor_admin only."
      plannedWork={[
        "Per-weekday working-hours template editor",
        "Preview of the slots the template produces",
      ]}
    />
  );
}
